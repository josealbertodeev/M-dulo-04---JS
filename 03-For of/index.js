// Estrutura de Repetição - Loop
// FOR OF
// Podemos interar strings e arrays
// encerra com break e continue
// ou até o fim do array

const nome = "Beto";
const nomes = ["Beto", "Ana", "Carlos", "Diana"];

// nome da varivel = letra = Beto; 
for (const letra of nome) {
    console.log(letra);
}